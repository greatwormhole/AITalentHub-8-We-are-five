version: "3.3"

services:
  backend:
    build:
      context: ./src/backend
      dockerfile: Dockerfile
    env_file:
      - ./src/backend/.env
    ports:
      - "$BACKEND_PORT:8000"
    volumes:
      - ./backend:/app
    networks:
      - web

  frontend:
    build:
      context: ./src/frontend
      dockerfile: Dockerfile
    env_file:
      - ./src/frontend/.env
    ports:
      - "$FRONTEND_PORT:80"
    networks:
      - web

networks:
  web:
    driver: bridge